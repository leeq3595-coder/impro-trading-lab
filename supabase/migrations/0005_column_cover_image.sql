-- =========================================================
-- 칼럼 대표 이미지 (목록/카드용 썸네일)
-- 본문 중간 이미지·유튜브 삽입은 컬럼 추가 없이, 프론트에서 본문 텍스트 줄 단위로
-- 이미지/유튜브 URL을 감지해서 자동 렌더링하는 방식으로 처리해요 (별도 테이블/컬럼 불필요).
-- =========================================================
alter table public.columns
  add column if not exists cover_image_url text;
