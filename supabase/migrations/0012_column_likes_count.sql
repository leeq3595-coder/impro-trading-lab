-- =========================================================
-- 칼럼용 "좋아요 수" — 실제 회원이 누르는 버튼이 아니라, 관리자가 직접
-- 숫자를 입력해서 보여주는 표시용 카운트예요 (신뢰도/전환율용).
-- =========================================================
alter table public.columns
  add column if not exists likes_count integer not null default 0;
